import { WritingMode } from './types';

export const MENU_ACTIONS = 'actions';
export const MENU_EMOJIS = 'emojis';
export const MENU_PREVIEW = 'preview';
export const MENU_SHORTCUTS = 'shortcuts';

export const MODE_MESSAGE: WritingMode = 'message';
export const MODE_EMAIL: WritingMode = 'email';
export const MODE_PRIVATE_NOTE: WritingMode = 'private-note';

export const MAX_MENU_HEIGHT = 300;
