import { StyleSheet } from '../../hooks/useStyles';

export const styleSheetScrollWrapper: StyleSheet = () => ({
  wrapper: {
    height: '100%',
    position: 'relative',
    overflowY: 'auto',
  },
});
