import {
  DirectionProvider,
  DirectionProviderProps as BaseProps,
  DirectionContext,
} from 'aesthetic-react';

export { DirectionContext };

export type DirectionProviderProps = BaseProps;

export default DirectionProvider;
